# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/

# ===== 路径错误创建的目录（Definition Lock OD-005） =====
D:LiuHao-AI-OSsrc*/
AppData/

# ===== 敏感信息 =====
.env
.env.local
.env.*.local
*.key
*.pem
*.crt
secrets/

# ===== 数据库 / 临时文件 =====
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
verify_metrics.db
liuhao_ai_os.db

# ===== 运行时 / 测试产物（测试生成的记忆/审计/密钥数据，不纳入版本管理） =====
.pytest-tmp/
tests/_report*.txt
tests/kernels/_*.txt
memory_data/*.json
data/audit/*.json
data/security/*.json
data/model_registry.json
backup_*.sql
bandit-report.json

# ===== 本地工作数据（记忆 / 诊断截图） =====
.workbuddy/
l-core-ui-verification.png

# ===== IDE / 编辑器 =====
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ===== 日志 / 临时 =====
*.log
*.tmp
logs/*.log

# ===== CI/CD / 构建产物 =====
*.pyc
.pytest_cache/
.coverage
htmlcov/
.tox/
dist/
build/

# ===== 错位文件 =====
=1.0.0

# ===== 独立子项目（2026-09-06 并入，含各自的 git/配置，不纳入本仓库版本管理） =====
LIUHAO_AI_TEAM_V1.0/
# 十源 packages 层是本项目核心架构层（三层骨架之二），纳入本仓库；
# 其余 LiuHao-O 脚手架（docker-compose/infrastructure/migrations 等空壳）仍忽略。
LiuHao-O/*
!LiuHao-O/pyproject.toml
!LiuHao-O/packages/
# ===== 人类身份登记表（Policy C-7）=====
# 含真实人员姓名，且随部署主机而变 —— 属运维数据，不入库。
# 样例见 config/human_identities.example.json
config/human_identities.json

# ===== 发布包（由 scripts/build_cloud_bundle.py 生成）=====
# 它是 src/ 与前端 dist 的副本，入库只会带来 200 个必然漂移的冗余文件。
# 需要时重新生成即可：python scripts/build_cloud_bundle.py
deploy/cloud/

# ===== 发布工具状态（认领 appId ↔ 本地目录，含本机绝对路径）=====
# 发布后由工具写入，仅对本机有效：换机器/换目录都会重建。
.wbapp_*.genie

# ===== 运行期状态（身份库等）=====
# *.sqlite3 已在上面忽略；补上 SQLite WAL 模式的伴生文件。
data/
*.sqlite3-wal
*.sqlite3-shm
